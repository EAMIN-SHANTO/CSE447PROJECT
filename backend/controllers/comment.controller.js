export const listComments = async (req, res) => {
  res.status(501).json({ message: "Comment listing placeholder - Phase 1 complete" });
};

export const createComment = async (req, res) => {
  res.status(501).json({ message: "Comment creation placeholder - Phase 1 complete" });
};
