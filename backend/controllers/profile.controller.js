export const getMyProfile = async (req, res) => {
  res.status(501).json({ message: "Profile read placeholder - Phase 1 complete" });
};

export const updateMyProfile = async (req, res) => {
  res.status(501).json({ message: "Profile update placeholder - Phase 1 complete" });
};
