export const listBids = async (req, res) => {
  res.status(501).json({ message: "Bid listing placeholder - Phase 1 complete" });
};

export const createBid = async (req, res) => {
  res.status(501).json({ message: "Bid creation placeholder - Phase 1 complete" });
};
